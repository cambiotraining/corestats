---
title: "area_51"
---
info
```{r}
#| echo: false
#| message: false
#| results: hide
source(file = "setup.R")
```

```{python}
from dgplots import *
```

```{python}
oystercatcher_py = pd.read_csv("data/CS2-oystercatcher-feeding.csv")
```

```{python}
# define the model
model = smf.ols(formula= "feeding ~ C(site)", data = oystercatcher_py)
# fit the model
results = model.fit()
```

```{python}
#| eval: false
dgplots(results)
```

```{r}
#| echo: false
library(reticulate)
source_python("dgplots.py")

dgplot <- dgplots(results)
knitr::include_graphics(dgplot)
```


```{python}
state_py = pd.read_csv("data/CS3-statedata.csv")
```

```{python}
# define the model
model = smf.ols(formula= "population ~ area + murder", data = state_py)
# fit the model
lm_state = model.fit()
```

```{python}
#| eval: false
dgplots(lm_state)
```

```{r}
#| echo: false
library(reticulate)
source_python("dgplots.py")

dgplot <- dgplots(lm_state)
knitr::include_graphics(dgplot)
```
